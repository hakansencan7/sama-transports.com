# Use only while obtaining the first certificate. Replace with nginx.conf afterwards.
server {
    listen 80;
    listen [::]:80;
    server_name forms.sama-transports.com;
    location /.well-known/acme-challenge/ { root /var/www/letsencrypt; }
    location / { return 404; }
}
